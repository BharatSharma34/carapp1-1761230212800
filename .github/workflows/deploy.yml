name: Deploy App
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Sync Server
        run: |
          curl -X POST "https://voila-api.mmopro.in/sync-app" \
            -H "Content-Type: application/json" \
            -d '{"app": "carapp1-1761230212800", "repo": "https://github.com/BharatSharma34/carapp1-1761230212800.git", "github_token": "${{ secrets.PRIVATE_GITHUB_TOKEN }}"}'